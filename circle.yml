machine:
  node:
    version: 8.9.1

dependencies:
  override:
    - yarn
  cache_directories:
    # - "~/paratii-mediaplayer/node_modules"
test:
  override:
    - yarn lint
    - yarn run build
deployment:
  release:
    tag: /v[0-9]+(\.[0-9]+)*/
    commands:
      - yarn publish
